<% if current_user?(item.user) %>
  <%= link_to "delete", item, method:  :delete,
                              confirm: "Are you sure?",
                              title:   item.content %>
<% end %>
